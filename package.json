{
  "name": "event-registration-ejs",
  "version": "1.0.0",
  "description": "A basic event registration system using Node.js, Express, and EJS. The system allows users to view upcoming events and register for them. An admin dashboard allows event management (adding, editing, and deleting events). The system reads from and writes to a .txt file for storing event data.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^4.21.0",
    "express-ejs-layouts": "^2.5.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
